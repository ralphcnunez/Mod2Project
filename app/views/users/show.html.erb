<h1><%= @user.name %></h1>

<%@user.avatars.each do |avatar|%>
<li><%= link_to avatar.name, user_avatar_path(@user, avatar)%></li>
<%end%>

<%= link_to "Create Avatar", new_user_avatar_path(@user) %>
<%= button_to "Delete Account", @user, :method => :delete %>
